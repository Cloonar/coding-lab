# build outputs
/bin/
/lab
/labctl
containers/agent-tools/artifacts/
web/dist/
internal/webui/dist/
web/node_modules/
# `mkdocs build` output; the site is published from CI, never from the repo.
# The bytecode cache is mkdocs importing mkdocs_hooks.py — same build residue.
/site/
__pycache__/

# playwright
web/e2e/.run/
web/test-results/
web/playwright-report/

# nix
result
result-*
.direnv/

# local databases
*.db
*.db-wal
*.db-shm
